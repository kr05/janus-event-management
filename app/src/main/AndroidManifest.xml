<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ritziercard9.projectjanus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".EventsListActivity"
            android:label="Eventos"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".EventDetails" android:parentActivityName=".EventsListActivity"/>
        <activity android:name=".ActivateTicketsActivity" android:label="Activación" android:parentActivityName=".EventDetails"/>
        <activity
            android:name=".ScannerEventDetailsActivity"
            android:label="Detalles del evento"
            android:parentActivityName=".ScannerEventsListActivity" />
        <activity android:name=".ValidateTicketsActivity" android:parentActivityName=".ScannerEventDetailsActivity"/>
        <activity
            android:name=".ScannerEventsListActivity"
            android:label="Eventos"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>